namespace Utils {
    // export var deepCopy = (array: any[]) => {
    //     let deepCopy = (array: any[]) => {
    //         var len = array.length;
    //         var copy = new Array(len);
    //         for (var i = 0; i < len; i++) {
    //             if (Array.isArray(array[i])) {
    //                 copy[i] = deepCopy(array[i]);
    //             }
    //             else {
    //                 copy[i] = array[i];
    //             }
    //         }
    //     }

    //     return deepCopy(array);
    // }
}
